var ISIScroller = function (t) {
	var i, n, o, e, l, c, s, a = void 0 == t || t,
		r = 30,
		u = !1,
		h = !1;
	this.start = function () {
		i = document.getElementById("isi"), n = document.getElementById("isi-container"), (window.location.host.toString().indexOf(".stage.") > -1 || window.location.host.toString().indexOf("localhost") > -1) && window.location.hash.toString().indexOf("isi") > -1 && (document.body.className += "show-isi", h = !0), g(), a && this.setScrollDelay(15)
	}, this.setScrollDelay = function (t) {
		a && (clearTimeout(e), o = 1e3 * t, e = setTimeout(I, o))
	}, this.setScrollDuration = function (t) {
		r = t, g()
	}, this.pauseISIScroll = function () {
		u = !0
	}, this.resumeISIScroll = function () {
		u = !1
	};
	var d = function () {
			return h ? void f() : (Math.abs(s - n.scrollTop) > 4 * l && f(), void(s >= 0 && Math.abs(s) < i.clientHeight - 20 ? n.scrollTop = s : s > i.clientHeight - 10 && (s = 0, f(), d(s))))
		},
		f = function () {
			clearTimeout(e), clearInterval(c)
		},
		I = function () {
			clearInterval(c), c = setInterval(S, 100, l)
		},
		S = function (t) {
			u || (Math.abs(s - parseInt(i.style.marginTop)) > t && f(), s || (s = 0), s += t, d(s))
		},
		g = function () {
			l = (i.clientHeight - n.clientHeight) / (10 * r)
		}
};
